version: 2
updates:
  # No github-actions entry: CI is intentionally manual (scripts/check.py) and
  # the repository has no .github/workflows, so that ecosystem fails every run.
  - package-ecosystem: uv
    directory: /
    schedule:
      interval: monthly
    groups:
      python-dependencies:
        patterns: ["*"]
